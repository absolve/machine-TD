# Machine-TD 美术风格规范

> 状态：**UI 主题已落地**（2026-09-13，v2.0 深银金属）
> 参考资源：`E:\参考图片资源\factory asset v.2 - chemical lab`（13 张像素图，已用脚本提取真实调色板）
> 预览：[`design/art_style_ui.png`](design/art_style_ui.png)（提案）、[`design/shots/`](design/shots/)（游戏内实拍）

---

## 1. 一句话定位

**亮着灯的现代化装配车间** —— 不是废弃工厂（锈蚀、昏暗、破损），
而是干净、有人维护、照明充足的量产车间。所有关卡都发生在同一座工厂的不同分区里。

UI 走**深银金属**：像被磨过的钢制仪表盘，不是白色塑料，也不是纯黑军事风。

## 2. 三条支柱

| 支柱 | 含义 | 具体做法 |
| --- | --- | --- |
| **深银为骨** | 所有结构面都是中低明度的蓝灰金属 | 面板 / 按钮 / 边框全部走单一条金属阶梯，**靠明度分层，不靠颜色** |
| **安全黄为魂** | 只留一个高饱和主色 | 安全黄只给"我方 / 选中 / 可操作 / 关键数值"。工业语境里黄色天然等于"注意这里" |
| **世界亮、UI 沉** | 场景中性灰，HUD 压暗 | 车间基色 `#8C9195`（明度 0.28），模态面板 `#242A36`（明度 0.02）。明度差 0.26，靠明度分层而不是靠投影硬拉 |

### 为什么是"深银"而不是纯黑或纯白

- **纯黑的军事风**：和"照明充足的车间"设定矛盾，长时间看也累。
- **纯白的浅色**：实测过一版，整个 UI 太飘、缺乏机械的重量感，也压不住场景。
- **深银金属**：参考包里 85% 以上的像素都集中在中低明度的蓝灰阶梯上，
  这是"金属被环境光照亮"的真实分布。

---

## 3. 色彩系统

调色板不是拍脑袋定的，是从参考图里**提取**出来的：对 13 张图的 135,456 个不透明像素
做 5bit 量化直方图 + k-means 聚类，整体平均明度 **0.336**。k-means k=10 的结果：

```
#040506  29.2%   描边 / 阴影（近黑）
#3a404f  18.0%   深蓝灰金属   ← 场景主体暗部
#636871  19.8%   中蓝灰金属   ← 场景主体
#8c9195  11.1%   亮银灰
#b5bdb1   8.4%   浅银（带轻微绿调）
#e4ebdf   3.7%   镜面高光（绿白）
#2c7b10 / #6bc23e   化学绿
#a95035             锈红
#48b2a8             青
```

### 3.1 金属阶梯（UI 骨架）

**加粗**的是参考图聚类原色，其余是在阶梯上插的中间档（用于更细的分层）：

| Token | 色值 | 来源 | 用途 |
| --- | --- | --- | --- |
| `metal_950` | `#0D1016` | 插值 | Tooltip 底、最深描边 |
| `metal_900` | `#1A1F28` | 插值 | 凹陷槽 / 输入框底 / 滚动条轨道 |
| `metal_800` | `#242A36` | 插值 | **模态窗口底**（弹窗、菜单） |
| `metal_700` | **`#3A404F`** | 聚类 | **地图内仪表盘底** |
| `metal_600` | `#4C5261` | 插值 | **按钮常态 / 抬升面** |
| `metal_500` | **`#636871`** | 聚类 | 按钮悬停、主边框 |
| `metal_400` | `#7B8189` | 插值 | 亮边框、滚动条抓手 |
| `metal_300` | **`#8C9195`** | 聚类 | 分隔线、地图内面板 2px 亮边框、**世界基色 / 清屏色** |
| `metal_200` | `#A8AFA8` | 插值 | **次要文字**（带绿调） |
| `metal_100` | **`#B5BDB1`** | 聚类 | 最亮面 / 我方塔机体（带绿调） |
| `metal_050` | **`#E4EBDF`** | 聚类 | **主文字 / 高光**（带绿调） |
| `metal_025` | `#F2F6EF` | 插值 | 悬停/按下时的文字提亮 |

> 亮部三档（`#A8AFA8` / `#B5BDB1` / `#E4EBDF`）刻意保留了参考图里那一点点**绿味**。
> 这是这个资源包的特征，去掉就变成普通中性灰了。

### 3.2 主强调 · 安全黄

| Token | 色值 | 用途 |
| --- | --- | --- |
| `hazard_700` | `#8A6A00` | 深色描边 |
| `hazard_600` | `#C99700` | 按下态边框 |
| `hazard_500` | **`#FFC61A`** | **主强调**：主按钮、选中态、关键数值、我方、经验条、波次条 |
| `hazard_300` | `#FFD964` | 悬停高光、滑块高亮 |

### 3.3 语义色（取自参考包的点缀色）

| Token | 色值 | 来源 | 用途 |
| --- | --- | --- | --- |
| `danger` | `#A95035` | 聚类锈红 | 危险按钮底、血条填充 |
| `danger_text` | `#F2765C` | 提亮 | 深底上的危险**文字**（数值 / 警告） |
| `success` | `#4AA222` | 聚类绿(深) | 通关、资金 |
| `success_lt` | `#6BC23E` | 聚类绿 | 高亮 / 单位类型标签 |
| `info` | `#48B2A8` | 聚类青 | 信息、能量、EMP |
| `info_lt` | `#7FD0C6` | 提亮 | 分区标题、面板名 |
| `gem` | `#6FD6EA` | 提亮 | 宝石（高级货币） |
| `warn` | `#E08A2E` | 插值 | 自爆 / 低血量 |

### 3.4 文字对比度（实测，WCAG 正文需 ≥ 4.5，大字 ≥ 3.0）

在**模态面板** `#242A36` 上：

| 组合 | 对比度 | 判定 |
| --- | --- | --- |
| 主文字 `metal_050` `#E4EBDF` | 11.81 | ✅ |
| 次要 `metal_200` `#A8AFA8` | 6.42 | ✅ |
| 压暗 `metal_400` `#7B8189` | 3.66 | ⚠️ 仅大字/禁用 |
| 强调黄 `#FFC61A` | 9.15 | ✅ |
| 危险文字 `#F2765C` | 5.15 | ✅ |
| 信息青 `#48B2A8` | 5.63 | ✅ |
| 宝石 `#6FD6EA` | 8.56 | ✅ |
| 成功绿 `#4AA222` | 4.44 | ⚠️ 贴近线，大字用 |

在**地图内面板** `#3A404F` 上：

| 组合 | 对比度 | 判定 |
| --- | --- | --- |
| 主文字 `#E4EBDF` | 8.39 | ✅ |
| 次要 `#A8AFA8` | 4.56 | ✅ |
| 强调黄 `#FFC61A` | 6.50 | ✅ |
| 信息青 `#48B2A8` | 4.00 | ⚠️ 仅大字 |
| 危险 `#F2765C` | 3.66 | ⚠️ 仅大字 |

在**按钮** `#4C5261` 上：

| 组合 | 对比度 | 判定 |
| --- | --- | --- |
| 常态文字 `#E4EBDF` | 6.34 | ✅ |
| 悬停/按下 `#F2F6EF` | 7.07 | ✅ |
| 黄底深字 `#1A1F28` on `#FFC61A` | 10.31 | ✅ |

> **两条硬规则**：① 浅底（黄/绿/青）永远配深字，绝不配白字；
> ② 地图内面板比模态面板亮一档，所以地图内面板上的小字要降到"仅大字"级别或用更亮的 token。

---

## 4. UI 组件规范

| 项 | 取值 | 说明 |
| --- | --- | --- |
| 圆角 | 按钮 `6` / 面板 `8` | 工业件不该太圆 |
| 边框 | `1px`（模态面板）/ `2px`（地图内面板、卡片） | 地图内面板靠粗亮边和模态区分 |
| 模态面板 | `metal_800` + 1px `metal_500` + 阴影 `#0D1016` 50% / 14px / y+3 | |
| 地图内面板 | `metal_700` + 2px `metal_300` + 阴影 45% / 12px / y+3 | 像被照亮的仪表 |
| 凹陷槽 | `metal_900` | 进度条轨道、滚动条轨道、输入框 |
| 正文 | `metal_050`，次要 `metal_200` | |
| 分隔线 | `metal_500`（1px 实色） | |

### 按钮三档

| 档位 | 底 | 边框 | 文字 | 用途 |
| --- | --- | --- | --- | --- |
| **主操作** | `hazard_500` | `hazard_600` | `metal_900` | 开始战斗、确认、下一关 |
| **次操作** | `metal_600` | `metal_400` | `metal_050` | 继续、取消、设置 |
| **危险** | `danger` | 深锈红 | `metal_025` | 返回主菜单、放弃、出售 |
| 禁用 | `metal_700` | `metal_600` | `metal_400` | — |

- 悬停：次操作底升到 `metal_500`，边框转 `hazard_500`，并带一层黄色柔光
- 按下：底压到 `metal_900`，去掉投影（模拟物理下压）
- 焦点：`hazard_500` 2px 外描边

### 状态条
轨道 `metal_900`，填充：基地生命 = `danger`、升级经验 = `hazard_500`、合成波次 = `hazard_500`。

### Tooltip
比 UI 再深一档：`metal_950` + 1px `metal_600`。在整体已经是深金属的界面上，
靠"更深一层 + 更强投影"跳出来。

### 模态遮罩
`rgba(13,16,22,0.60)`。世界是亮的，遮罩压暗后弹窗边缘更利落。

---

## 5. 世界元素规范

世界基色用参考包的 `#8C9195`（聚类里占 11.1% 的那档，明度 0.28），**并且与清屏色取同一个值**：
背景贴图（欢迎页 / 选关）和战斗地图没有地形的区域是同一个底色，视觉上完全连贯。

明度关系：世界 0.28 → 地图内面板 0.05（差 0.23）→ 模态面板 0.02（差 0.26）。
三者拉开三档，靠明度分层，不用靠投影硬拉。

| 元素 | 色值 | 说明 |
| --- | --- | --- |
| 车间基色（占位） | `#8C9195` | 参考色，背景图与清屏色同值 |
| 伸缩缝 | 比基色暗一档 | 让大块地面不空 |
| **流水线皮带** | `#1A1F28` 深 + 底部更暗 | **敌人前进路线** |
| 流水线箭头 | `#FFC61A` 人字形 | 明确行进方向 |
| 流水线护栏 | `#8C9195` | |
| 危险条纹 | `#FFC61A` / `#0D1016` 45° | 入口、出口门前、禁区 |
| 可建造基座 | 浅青灰半透明 + 四角黄螺栓 | 塔位 |
| 出口 | 门框 `#8C9195` + 绿色 `#4AA222` 灯箱 | 敌人离场点 / 基地判定点 |

### 为什么流水线是最深的一块

基色明度 0.28、皮带明度 0.015 —— **全画面对比度最高的元素就是敌人路径**。
这是刻意的：塔防的第一信息是"敌人在哪、往哪走"，它必须压过所有装饰。

### 关卡模板（之后所有工厂图都按这套拼）

```
地面层   → 基色 + 伸缩缝
装饰层   → 管道、集装箱、控制室隔断、警示牌（不占格）
路径层   → 流水线段（可拐弯、可分支、可合并）
基座层   → 可建造板
出口层   → 出口门 + 危险区
```

以后要做"港口 / 矿区 / 实验室"变体，**只换地面材质、道具外形和装饰色相，UI 完全不动**。

---

## 6. 单位识别规范

| 阵营 | 机体 | 描边 | 强调 |
| --- | --- | --- | --- |
| **我方塔** | `metal_100` `#B5BDB1` 亮银 | `metal_700` | `hazard_500` 黄 |
| **敌方** | `metal_700` `#3A404F` 深金属 | `#0D1016` | `danger` 锈红 |

全局铁律：**黄 = 我方，锈红 = 敌方**。选中的塔加黄色外圈，敌方血条统一锈红。

单位类型标签色：重装 = `metal_400`，空中 = `info`，维修 = `success_lt`，
自爆 = `warn`，控制 = `#8E6BD9`，精英 = `hazard_500` 金边。

---

## 7. 字体与图标

**字体**：现在只有 `AlibabaPuHuiTi-3-85-Bold` 一个字重。
深底亮字比浅底深字更能承受全 Bold，但仍建议补一档 Regular 给正文与说明。

**图标**：现有图标风格混杂（线稿、实心、彩色混在一起）。统一为 2px 等宽描边的工业线稿，
颜色 `metal_200`，激活态转 `hazard_500`。

欢迎页四个按钮的图标色已按语义统一：

| 按钮 | 色 |
| --- | --- |
| 教程 | `#48B2A8` 青（信息） |
| 开始游戏 | `#FFC61A` 安全黄（主操作） |
| 设置 / 关于 | `#A8AFA8` 银（次要） |

---

## 8. 落地顺序

1. ✅ **UI 主题迁移到深银金属**（2026-09-13 v2.0，见 §9 / §10）
2. ⬜ **加一档正文字重**并调整字号阶梯
3. ⬜ **统一图标**（先统一颜色，再逐个重绘）
4. ⬜ **世界美术**：工厂地面 / 流水线 / 出口 / 基座的贴图与 TileSet
5. ⬜ 用新规范重做 `level_1` 作为样板关，再铺开其余关卡

---

## 9. 颜色迁移对照（v1.0 亮色 → v2.0 深银）

v1.0 过亮（面板明度 0.94），v2.0 整体压到中低明度并换成金属色相。

| 用途 | v1.0 亮色 | v2.0 深银 | Token |
| --- | --- | --- | --- |
| 模态面板底 | `#F7F9FA` | `#242A36` | `metal_800` |
| 地图内面板底 | `#F7F9FA` | `#3A404F` | `metal_700` |
| 按钮常态 | `#EDF1F4` | `#4C5261` | `metal_600` |
| 按钮悬停 | `#FFF3C4` | `#636871` + 黄边 | `metal_500` |
| 凹陷槽 | `#DDE4E9` | `#1A1F28` | `metal_900` |
| 边框 | `#C6D0D8` | `#636871` | `metal_500` |
| 主文字 | `#1F262C` | `#E4EBDF` | `metal_050` |
| 次要文字 | `#56626C` | `#A8AFA8` | `metal_200` |
| 压下文字 | `#7A8892` | `#7B8189` | `metal_400` |
| 强调文字 | `#8A6A00` | `#FFC61A` | `hazard_500` |
| 危险 | `#E03B3B` | 底 `#A95035` / 字 `#F2765C` | `danger` |
| 成功 | `#2E9E52` | `#4AA222` | `success` |
| 信息 | `#1C74C4` | `#48B2A8` | `info` |
| 宝石 | `#0E7C93` | `#6FD6EA` | `gem` |
| 世界基色 | `#CFCBC4` 暖灰 | `#8C9195` 金属灰 | `metal_300` |
| 模态遮罩 | `rgba(31,38,44,0.45)` | `rgba(13,16,22,0.60)` | `metal_950` |

### 世界背景的处理

`shader/background.gdshader` 把原始贴图当作"细节图案"叠在 `base_color` 上，
贴图自身明暗不决定整体亮度 —— 这样换成任意底图都不会把画面带黑。
`project.godot` 的 `rendering/environment/defaults/default_clear_color` 与 `scene/bg.tscn` 的
`base_color` **取同一个值 `#8C9195`**（有断言校验），所以欢迎页、选关页与战斗地图的底色完全一致。

---

## 10. 迁移验证（2026-09-13，真实运行）

| 检查项 | 方法 | 结果 |
| --- | --- | --- |
| 主题文字色 | 遍历 `theme.tres` 全部 18 个 `*/font_color` | 全部为亮色；禁用/加速键/分隔符为压暗中间调 |
| StyleBox 面色 | 遍历 `theme/style/*.tres` | 20 个"面"全部为深金属（明度 0.005~0.137）；6 个填充色（滑块×2、经验/血条、音量条）单列 |
| **调色板溯源** | 把主题面色投影到参考图聚类色构成的明度折线上，算最大通道偏差 | 面板底 0.009、按钮底 0.007、按钮悬停 0.000、按下 0.008、地图内面板 0.007、小标签 0.000 —— **全部 ≤0.009** |
| 面板实拍 | 渲染后取面板内像素**中位数**（避开文字/按钮） | 暂停 0.084 / 设置 0.024 / 结算 0.023 / 塔详情 0.053 / 敌人详情 0.053 |
| 文字可读性 | 同一次采样取 **98 分位**作为"最亮文字" | 0.418 ~ 0.812，均显著高于面色 |
| 世界基色 / 清屏色 | 读 `ProjectSettings` 与 `bg.tscn` 的 `base_color` | 均为 `#8C9195`，**完全一致**；世界 0.280 vs 模态面板 0.023（差 0.257）/ 地图内面板 0.051（差 0.229） |
| 整屏明度 | 欢迎页整屏平均 | **0.327**（世界中性灰，UI 沉下去，整体不压黑） |
| 转场 / 弹窗层级 | 重跑既有两套回归 | 45 + 26 项断言，**0 失败** |

**改动范围**：`theme/theme.tres`、`theme/style/` 全部 31 个 StyleBox、
15 个 `.tscn` 的硬编码文字色与内联 StyleBox、`achievement_panel.gd` / `level_intro_panel.gd`
的颜色常量、`background.gdshader` + `bg.tscn` + `project.godot` 的世界基色。
**没有任何 `.tscn` 的结构改动**，纯换皮。

截图见 [`design/shots/`](design/shots/)。

---

## 11. 下一步待确认

- [ ] 现在这个深度合适吗？如果还想再深/再浅，改 `metal_800`（模态）和 `metal_700`（地图内）两档即可
- [ ] 主强调色维持安全黄 `#FFC61A`，还是改用参考包的化学绿 `#6BC23E`？（绿会与"成功/资金"语义打架，我倾向保留黄）
- [ ] 下一步优先做**世界美术**（流水线 / 出口 / 基座的贴图与 TileSet）还是**字重 + 图标**？
