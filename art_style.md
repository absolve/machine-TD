# Machine-TD 美术风格规范

> 状态：**UI + 地板配色已落地**（2026-09-13，v3.2 深银金属 + 深色地砖）
> 参考资源：
> - `E:\参考图片资源\factory asset v.2 - chemical lab`（13 张，金属阶梯来源）
> - `E:\参考图片资源\Tech Dungeon Roguelite - Asset Pack (DEMO)\tileset x3.png`（地板砖来源）
>
> 预览：[`design/art_style_ui.png`](design/art_style_ui.png)、[`design/art_style_world.png`](design/art_style_world.png)、
> 实拍 [`design/shots/`](design/shots/)

---

## 1. 一句话定位

**亮着灯的现代化装配车间** —— 不是废弃工厂（锈蚀、昏暗、破损），
而是干净、有人维护、照明充足的量产车间。所有关卡都发生在同一座工厂的不同分区里。

地面是**深蓝灰地砖**（`#333C57` + `#1A1C2C` 砖缝），UI 是**深银金属仪表盘**（`#566C86` + `#94B0C2` 亮边）。

## 2. 三条支柱

| 支柱 | 含义 | 具体做法 |
| --- | --- | --- |
| **单色板** | 地板与 UI 出自同一个色板 | 都用 Sweetie 16 的灰阶（`#1A1C2C`→`#333C57`→`#566C86`→`#94B0C2`→`#F4F4F4`），中间档用插值补齐 |
| **安全黄为魂** | 只留一个高饱和主色 | 安全黄只给"我方 / 选中 / 可操作 / 关键数值"。工业语境里黄色天然等于"注意这里" |
| **暗场 + 亮框** | 地板压暗，UI 靠在上面 | 地板 0.046 / 面板 0.144 / 边框 0.412（WCAG 相对明度）。面板底比地板亮一档，靠**高对比亮边框**把面板"切"出来 |

### 为什么是"深银"而不是纯黑或纯白

- **纯黑的军事风**：和"照明充足的车间"设定矛盾，长时间看也累。
- **纯白的浅色**：实测过一版（v1.0，面板明度 0.94），整个 UI 太飘、缺乏机械重量感，也压不住场景。
- **深银金属**：参考包里 85% 以上的像素都集中在中低明度的蓝灰阶梯上，
  这是"金属被环境光照亮"的真实分布。

---

## 3. 色彩系统

### 3.1 地板（来自 Tech Dungeon tileset 最右区域）

对 `tileset x3.png`（3552×2208）做列占用分析，找到最右侧的独立区块
**x 3072..3362 / y 96..287**，内容是 3×2 块地砖，只有两个颜色：

| Token | 色值 | 占比 | 用途 |
| --- | --- | --- | --- |
| `floor_tile` | **`#333C57`** | 50.8% | 地砖主色 **= 背景基色 = 清屏色** |
| `floor_grout` | **`#1A1C2C`** | 15.7% | 砖缝 / 描边（其余 33.5% 是透明） |

这两个色属于 **Sweetie 16** 调色板。砖格边长设为 **64px**，与游戏的建造网格
（`StageData.TileSize`）对齐，地板缝和塔位网格天然重合。

### 3.2 金属阶梯（UI 骨架，同一色板）

**加粗**的是 Sweetie 16 原色，其余是折线上的插值档：

| Token | 色值 | 来源 | 用途 |
| --- | --- | --- | --- |
| `ui_950` | **`#1A1C2C`** | 原色 | 凹陷槽 / 进度条轨道 / 按钮按下 / Tooltip 底 |
| `ui_800` | **`#333C57`** | 原色 | 面板内小标签（chip）/ 禁用按钮 / 滚动条轨道 |
| `ui_750` | `#44586E` | 插值 | 按钮常态 |
| `ui_720` | `#4A5F78` | 插值 | 小标签边框 |
| `ui_600` | **`#566C86`** | 原色 | **模态面板底 / 地图内面板底 / 卡片底** |
| `ui_500` | `#6F8AA4` | 插值 | 按钮悬停（"点亮"态） |
| `ui_450` | `#7F96A8` | 插值 | 分隔线 / 滚动条抓手 |
| `ui_400` | **`#94B0C2`** | 原色 | **面板 2px 亮边框** / 次要强调 |
| `ui_300` | `#9FB4C4` | 插值 | 压暗文字（禁用 / 加速键） |
| `ui_100` | `#DCEAF2` | 插值 | **次要文字** |
| `ui_050` | `#F8FBFD` | 插值 | **主文字** |
| `ui_000` | **`#F4F4F4`** | 原色 | 最亮（悬停/按下文字） |

> **一条硬规则**：所有 UI 色必须落在这条折线上（测试会算"到折线最近点的最大通道偏差"，
> 要求 ≤0.025）。目前全部 ≤0.012。

### 3.3 主强调 · 安全黄

| Token | 色值 | 用途 |
| --- | --- | --- |
| `hazard_700` | `#8A6A00` | 深色描边 |
| `hazard_600` | `#C99700` | 按下态边框 |
| `hazard_500` | **`#FFC61A`** | **主强调**：主按钮、选中态、关键数值、我方、经验条、波次条 |
| `hazard_300` | `#FFD964` | 悬停高光、滑块高亮 |

### 3.4 语义色

| Token | 色值 | 用途 |
| --- | --- | --- |
| `danger` | `#A95035` | 危险按钮底、血条填充 |
| `danger_text` | `#FFB3A0` | 面板上的危险**文字** |
| `success` | `#4AA222` | 通关、资金（填充） |
| `success_lt` | `#A7F070` | 面板上的成功**文字** |
| `info` | `#48B2A8` | 信息 / 能量（填充） |
| `info_lt` | `#7FD0C6` | 面板上的信息**文字** |
| `gem` | `#6FD6EA` | 宝石 |
| `warn` | `#E08A2E` | 自爆 / 低血量 |

### 3.5 文字对比度（实测，WCAG 正文需 ≥ 4.5，大字 ≥ 3.0）

**在面板 `#566C86` 上：**

| 组合 | 对比度 | 判定 |
| --- | --- | --- |
| 主文字 `#F8FBFD` | 5.20 | ✅ |
| 次要 `#DCEAF2` | 4.40 | ⚠️ 贴近线 |
| 压暗 `#9FB4C4` | 2.52 | 仅禁用态（刻意压低） |
| 强调黄 `#FFC61A` | 3.43 | ⚠️ 仅大字 |
| 危险 `#FFB3A0` | 3.14 | ⚠️ 仅大字 |
| 信息青 `#7FD0C6` | 3.02 | ⚠️ 仅大字 |
| 成功绿 `#A7F070` | 3.95 | ⚠️ 仅大字 |
| 宝石 `#6FD6EA` | 3.21 | ⚠️ 仅大字 |

> **面板是中性灰，留给浅色文字的空间本来就窄** —— 只有"接近白"的色才能过 4.5。
> 目前所有语义色（黄/红/青/绿）都只在**大号粗体数值**上使用（字号 28~34、全 Bold），
> 满足大字 3.0 的门槛。如果以后要放正文，需要走深层变体或在面板上加暗底衬。
> **这也是"补一档 Regular 字重"排在待办里的原因** —— 单靠颜色已经分不出层级了。

### 3.6 地板 / 面板 / 边框的明度关系

| 层 | 色值 | WCAG 相对明度 |
| --- | --- | --- |
| 地板 | `#333C57` | 0.046 |
| 面板底 | `#566C86` | 0.144 |
| 面板亮边框 | `#94B0C2` | 0.412 |

地板→面板差 **0.098**，面板→边框差 **0.268**。三层拉开，不靠投影硬拉。

---

## 4. UI 组件规范

| 项 | 取值 | 说明 |
| --- | --- | --- |
| 圆角 | 按钮 `6` / 面板 `8` | 工业件不该太圆 |
| 边框 | `2px`（面板、卡片）/ `1px`（按钮、小标签） | 面板一律用 `#94B0C2` 亮边 |
| 面板底 | `#566C86` @0.97 + 2px `#94B0C2` + 阴影 `#0D1016` 50% / 14px / y+3 | 模态与地图内面板同色，靠内边距与投影区分 |
| 凹陷槽 | `#1A1C2C` | 进度条轨道、滚动条轨道、输入框 |
| 面板内小标签 | `#333C57` + `#4A5F78` 边 | 形成"浅面板 + 深凹槽"的仪表感 |
| 正文 | `#F8FBFD`，次要 `#DCEAF2` | |
| 分隔线 | `#7F96A8`（1px 实色） | |

### 按钮三档

| 档位 | 底 | 边框 | 文字 | 用途 |
| --- | --- | --- | --- | --- |
| **主操作** | `#FFC61A` | `#C99700` | `#1A1C2C` | 开始战斗、确认、下一关 |
| **次操作** | `#44586E` | `#7F96A8` | `#F8FBFD` | 继续、取消、设置 |
| **危险** | `#A95035` | 深锈红 | `#F4F4F4` | 返回主菜单、放弃、出售 |
| 禁用 | `#333C57` | `#4A5F78` | `#9FB4C4` | — |

- 悬停：底升到 `#6F8AA4`，边框转 `#FFC61A`，带一层黄色柔光（"点亮"）
- 按下：底压到 `#1A1C2C`，去掉投影（模拟物理下压）
- 焦点：`#FFC61A` 2px 外描边

### 状态条
轨道 `#1A1C2C`，填充：基地生命 = `#D95A3C`、升级经验 = `#FFC61A`、波次 = `#FFC61A`。

### Tooltip
`#1A1C2C` 底 + `#94B0C2` 90% 边 + 强投影。

### 模态遮罩
`rgba(13,16,22,0.60)`。

### 危险条纹带（滚动组件）

`scene/ui/hazard_strip.tscn` —— `TextureRect` + `shader/hazard_scroll.gdshader`。

条纹不是静态的：UV 沿 X 轴每帧偏移，斜条横向流动，看起来像一条在跑的警示胶带。
贴图 `sprite/hazard_strip.png` 是 **272×22**，横向严格无缝（宽度取条纹周期 68px 的整数倍，
测试逐像素比对 4488 个点、零不匹配），所以滚动时看不到接缝。

| 参数 | 值 | 说明 |
| --- | --- | --- |
| `stretch_mode` | `TILE` | 平铺，条纹宽度保持 34px 不拉伸 |
| `speed` | `0.12` | 越大越快；给负值就反向流 |
| `alpha` | `1.0` | 整体透明度 |

放在欢迎页标题上下各一条（1200×22）。想换成别的色，直接改贴图里那两色即可。

---

## 5. 世界元素规范

| 元素 | 色值 | 说明 |
| --- | --- | --- |
| **车间地面** | `#333C57` | 清屏色；战斗地图没有地形的区域就是它 |
| **主菜单 / 选关背景** | `#313953` 实测 | `bg.tscn`：128px 双色棋盘格贴图平铺 + 垂直滚动（原有效果），色调经 `tint` 映射到地砖色 |
| **流水线皮带** | `#1A1C2C` 深 + 底部 `#0D1016` | **敌人前进路线** |
| 流水线箭头 | `#FFC61A` 人字形 | 明确行进方向 |
| 流水线护栏 | `#566C86` | |
| 危险条纹 | `#FFC61A` / `#0D1016` 45° | 入口、出口门前、禁区；欢迎页标题上下两条**带动画**（见下） |
| 可建造基座 | `#3D7A72` 台面 + `#6BC2B0` 亮边 + 四角 `#FFC61A` 黄螺栓 | 塔位；瓦片见下 `floor_slot.png` |
| 出口 | 门框 `#566C86` + 绿色 `#4AA222` 灯箱 | 敌人离场点 / 基地判定点 |

> **背景场景保持原样**：`bg.tscn` 一直是「一张平铺贴图 + UV 沿 Y 轴滚动 + 上暗下亮的纵向渐变」，
> 这次只加了 `tint` 把中性灰挪到蓝灰，滚动、平铺、渐变全都没动。
> `tint` 的数值是**实测标定**的（引擎采样纹理时会做自己的色彩空间处理，
> 按 PNG 里读到的平均色直接换算不对），详见 `shader/background.gdshader` 顶部注释。

### 为什么流水线是最深的一块

地砖 0.046、皮带 0.012 —— **路径比地板还深一档**，再加上 `#FFC61A` 人字形箭头，
敌人路径是全画面最抓眼的信息。这是刻意的：塔防的第一信息是"敌人在哪、往哪走"。

### 关卡模板（之后所有工厂图都按这套拼）

```
地面层   → 地砖 + 砖缝                    ✅ sprite/tile/floor_*（12 种）
装饰层   → 管道、集装箱、控制室隔断、警示牌（不占格）   ⬜ 未做
路径层   → 流水线段（可拐弯、可分支、可合并）           ✅ sprite/tile/belt_*（12 个方向）
基座层   → 可建造板                        ✅ sprite/tile/floor_slot.png
出口层   → 出口门 + 危险区                  ⬜ 未做
```

以后要做"港口 / 矿区 / 实验室"变体，**只换地面材质、道具外形和装饰色相，UI 完全不动**。

### 车间地面瓦片集（`sprite/tile/`，64px）

关卡地面用 64×64 瓦片（= `StageData.TileSize`），入口是 `scene/level/factory_floor.tres`：
`tile_size = 64×64`，**一张 PNG 一个 `TileSetAtlasSource`** —— 以后新增地板类型只要丢一张
64×64 的图进 `sprite/tile/`，再往 `.tres` 里加一条 `[sub_resource]` + `sources/N` 即可，
不用重排图集。

每张瓦片只允许用 §3.1 / §3.3 的地板档位：

| 色 | 角色 |
| --- | --- |
| `#333C57` | 地板面（必须占主导） |
| `#1A1C2C` | 砖缝 / 凹槽 / 描边 |
| `#0D1016` | 深凹槽（警示带底色） |
| `#272C42` | 插值档，**只用于油渍这类"半深"细节** |
| `#FFC61A` + `#0D1016` | 警示带 45° 条纹 |

四周保留 1px `#1A1C2C` 描边 —— **平铺后相邻两格自然合成 2px 砖缝**，全屏的网格感就是这么来的。

| 文件 | 名称 | 怎么用 |
| --- | --- | --- |
| `floor_base.png` | 基础底板 | 铺底，占 ~80% |
| `floor_vent.png` | 通风格栅 | 沿墙成排，做墙脚细节带 |
| `floor_drain.png` | 排水格栅 | 一格一个零星点，**别连片** |
| `floor_bolt.png` | 螺栓底板 | 四角螺栓；平铺后四格在交叉点汇成一组，像拼装钢板 |
| `floor_tread.png` | 花纹钢板 | 防滑纹，适合铺成通道条带 |
| `floor_hatch.png` | 检修口 | 深色盖板 + 提手槽 |
| `floor_trench.png` | 排水沟 | 沿 X 轴连铺成一条贯通的沟 |
| `floor_sump.png` | 集水坑格栅 | 最深的一档，稀有使用 |
| `floor_stain.png` | 油渍 | 叙事细节，点缀 |
| `floor_crack.png` | 裂纹 | 叙事细节，沿 Y 轴接起来更长 |
| `floor_hazard.png` | 安全警示带 | 入口 / 出口 / 禁区前；**左右跨格连续**（45° 条纹周期 16 整除 64），故意断开竖直砖缝 |
| `floor_slot.png` | **塔位基座** | 可建造区。铺在 `allowArea` 的格子上，塔建在它上面 |

> **密度原则**：基础板负责底色，其他都是低占比点缀。1920×1080 实测约
> 基础 80% / 格栅类 8% / 其余合计 12% 时"有细节但不吵"；
> 格栅类连片超过 1/4 屏就开始和敌人路径抢视觉优先级。
>
> **不要**把地板螺栓做成黄螺栓 —— 四角黄螺栓是 §5「可建造基座」的专属记号，
> 地板上的螺栓一律用 `#1A1C2C` / `#272C42`，否则玩家会误读成塔位。

### 塔位基座 `floor_slot.png`（可建造区）

参考包 `Industrial&Underground2DMegaPropsPack_v1.0` 里的
`industrial_factory_fixtures_robotic_arm_industrial_01`（机械臂圆形底座，分层圆台 + 一圈铆钉）
与 `industrial_factory_fixtures_workbench_steel_vise_01`（工作台，受光顶面 + 亮边 + 青色钢架）。

读法取自工作台：**受光顶面 + 右下 3px 板厚 + 左上 1px 亮边**，一块"抬起来的厚钢板"。
外圈再放四角黄螺栓和四边中点安装块（后者来自手绘原稿那四个凸起，改成**内嵌**而不是外凸 ——
外凸的话相邻两个基座会撞在一起）。

配色不用单色而是一条 6 阶青色梯（由 §5 的 `#3D7A72` 与 `#6BC2B0` 插值而来）：

| 色 | 用途 |
| --- | --- |
| `#44867C` | 顶面（受光） |
| `#336860` | 板厚侧面 / 中央凹槽底 |
| `#56A294` | 四边安装块 |
| `#2A5952` | 凹槽暗部 |
| `#3D7A72` | 内圈台面 |
| `#6BC2B0` | 左上亮边 / 螺栓垫圈 |
| `#FFC61A` | 四角螺栓 + 安装块中心（= §6「黄 = 我方」） |

> **为什么基座是青不是绿**：试过手绘原稿的 `#6ABE30`，问题是**绿色防御塔站在绿基座上直接糊掉了**
> （见 `design/tile_base_slot_compare.png` 的 B 行）。青色对地板（`#333C57`）和三种塔身都有分离度，
> 而且正好是 §5 早已定下的基座色。
>
> 基座台面明度 **0.468**，地板 **0.235** —— 差一档，可建造区一眼可见；
> 同时比 UI 面板（`#566C86`）更没有"界面感"，因为它是世界里的实体。
>
> 基座瓦片的 1px 砖缝与 `floor_*` 完全一致，所以它直接铺进同一个 `bg` TileMapLayer 即可，
> 不需要额外图层。

### 流水线皮带瓦片 `belt_*`（路径层，12 个方向）

**敌人前进路线**。塔防的第一信息是"敌人在哪、往哪走"，所以这一段是全画面最抓眼的信息：
皮带 `#1A1C2C` 比地板 `#333C57` **深一档**（明度 0.113 vs 0.235），再用 `#FFC61A` 人字形箭头指方向。

生成方式不是手绘 12 张，而是**中心线折线 + 距离场**：

```
皮带面   d <= 18      （36px 宽）
护栏     18 < d <= 21 （`#566C86`，外侧一道 `#3E5566` 压暗）
其余                地板
```

- 折线从**入口边中点**经格子中心到**出口边中点**，长度恰好 64（直段）或 32+32（拐角）
- **相位 = 从入口边算起的弧长**；横向筋周期 16、人字形周期 32，都整除 64 →
  跨格图案天然连续（这是第一版最大的坑：每个文件各自从零算相位，拼起来箭头会撞成叉）
- 拐角的外侧自动被距离场倒成圆角，内侧保持直角
- 自检：12 件 × 4 边 = 48 条边，**24 条连通边的通道区间必须完全一致（现为 `11..52 / 42px`），
  24 条非连通边必须为 0**，否则拼接会错位

命名 = `belt_<入口><出口>`，**两个旋向的拐角都在**（8 个拐角）：

| 类 | 文件 | 流向 |
| --- | --- | --- |
| 直段 | `belt_we` `belt_ew` `belt_ns` `belt_sn` | 右 / 左 / 下 / 上 |
| 拐角（顺时针） | `belt_ws` `belt_wn` `belt_es` `belt_en` | 西→南 / 西→北 / 东→南 / 东→北 |
| 拐角（逆时针） | `belt_sw` `belt_se` `belt_nw` `belt_ne` | 南→西 / 南→东 / 北→西 / 北→东 |

> 铺图时按"上一格 → 本格 → 下一格"的方向选件：入口 = 指向前一格的方向，出口 = 指向后一格的方向。
> 效果见 [design/tile_belt_route.png](design/tile_belt_route.png)（1:1 见 `_1x` 版）。
>
> **注意**：现在关卡里的敌人是沿 `Path2D` 的 `Curve2D` 走的（`base_level.gd` 用
> `curve.get_baked_points()`），皮带瓦片只是**视觉标记**。要让两者严格对齐，
> 要么把路径规划改成沿格子中心走，要么改成按曲线动态画带子。**目前还没接线。**

---

## 6. 单位识别规范

| 阵营 | 机体 | 描边 | 强调 |
| --- | --- | --- | --- |
| **我方塔** | `#94B0C2` 亮银 | `#333C57` | `#FFC61A` 黄 |
| **敌方** | `#333C57` 深金属 | `#1A1C2C` | `#A95035` 锈红 |

全局铁律：**黄 = 我方，锈红 = 敌方**。注意地板本身就是 `#333C57`，
**敌方机体不能直接用 `#333C57`**，需要加 `#1A1C2C` 描边 + 锈红高亮，或整体再压暗一档。

单位类型标签色：重装 = `#9FB4C4`，空中 = `info_lt`，维修 = `success_lt`，
自爆 = `warn`，控制 = `#8E6BD9`，精英 = `#FFC61A` 金边。

### 塔的多层装配契约（画任何一座塔都必须遵守）

场景结构是 `Tower` → `base`(AnimatedSprite2D，**不旋转**) + `turret`(AnimatedSprite2D，**绕节点原点旋转**，
带 `Marker2D` 炮口) + `spark`(炮口闪光)。所以拆成两张图：

| 图 | 尺寸 | 旋转轴 | 朝向 | 说明 |
| --- | --- | --- | --- | --- |
| `sprite/tower/base_*.png` | **48×48**（比 64px 塔位瓦片小一圈） | — | 不旋转，必须各向同性 | 底座 |
| `sprite/tower/turret_*.png` | 64×64（长炮管的可到 80×64） | **贴图 (32,32)** | **炮管朝 +X** | 炮塔 |

注意点：

- **炮塔贴图必须朝右** —— 代码把 `turret.rotation` 直接设成目标角度。
- **非 64 宽的贴图要用 `offset` 把旋转轴挪回节点原点**。例：`turret_cannon.png` 是 80×64，
  轴在贴图 (32,32)，贴图中心是 (40,32)，所以 `turret.offset = Vector2(8, 0)`。
- **旋转体不能画死方向光**。只保留"左上受光 / 右下压暗"的径向渐变，不然转 180° 就露馅。
  已验：12 个角度逐一看过，无抖动、无穿帮。
- **`Marker2D` 炮口位置** = 炮口相对旋转轴的像素距离（机枪塔 ≈ `(31,0)`，加农炮 ≈ `(47,0)`）。
- 炮口闪光 `spark` 复用 `towerDefense_tile295.png`，不需要重画。

### 塔身配色

机体用 §6 的亮银阶梯，深色构件统一 `#333C57`，描边 `#1A1C2C`，**安全黄只做"我方记号"**
（螺栓 / 警示带 / 舱盖），不要整块涂黄：

| 色 | 用途 |
| --- | --- |
| `#A8C0CE` | 顶面高光 |
| `#94B0C2` | 顶面（= §6 我方塔亮银） |
| `#7590A4` | 侧面 |
| `#5E7A8C` | 暗侧面 / 炮管上缘 |
| `#333C57` | 炮管、格栅、深色构件 |
| `#1A1C2C` | 描边 |
| `#FFC61A` | 四角螺栓 / 警示环 / 舱盖把手 |

> **试过用户原稿的配色**（机枪塔灰 `#847E87` / 大炮绿 `#6ABE30`），对比见
> [design/tower_compare_palette.png](design/tower_compare_palette.png)：
> 灰机枪塔在青基座上发闷、绿大炮过饱和且和塔位基座抢色，
> 所以统一走亮银。**塔与塔之间靠剪影区分，不靠换色**（§6 铁律：黄=我方）。
> 变体 B 的图留在 `.td_verify/tower/*_B.png`，要换随时可以。

### 底座四种型（`sprite/tower/base_*.png`，均 48×48）

| 文件 | 形状 | 建议给谁 |
| --- | --- | --- |
| `base_round.png` | 圆台，分层 + 8 颗螺栓 | 通用（机枪塔 / 加农炮） |
| `base_octagon.png` | 八角板，8 颗螺栓 | 重型（加农炮） |
| `base_plate.png` | 方形切角钢板，四角黄螺栓 | 轻量（火箭 / EMP） |
| `base_tripod.png` | 三脚爪 + 中心毂 | 细高塔（特斯拉 / 激光） |

**大防御塔（2×2：无人机基地 / 特斯拉 / 激光）的底座另做**，尺寸要按 128px 走。

### 七座塔的炮塔读法

| 塔 | 尺寸 | 旋转 | 读法 |
| --- | --- | --- | --- |
| 机枪塔 `turret_machinegun.png` | 64×64 | ✅ 朝目标 | 圆炮塔 + 后部弹药舱 + 窄炮盾 + **上下两根炮管**（管身露出 20px、两管间留 9px 缝）。双管是本塔唯一身份特征，**炮盾绝不能长到把管子挡掉** |
| 加农炮 `turret_cannon.png` | 80×64 | ✅ 朝目标 | **坦克舱盖式炮身**：座圈 + 盖板 + 左侧两个合页耳 + **垂直于炮管的拉手**；一根等宽光管平口伸出 |
| 火箭塔 `turret_rocket.png` | 64×64 | ✅ 朝目标 | **爱国者发射架**：箱体压暗，里面 **4 根浅色发射管 + 4 个大管口**。管子必须比箱体亮，否则读成通风口 |
| EMP 雷达 `turret_emp.png` | 64×64 | ❌ 静态 | 抛物面天线（凹面由外到内 4 级压暗 + 左上高光）+ 中心馈源臂 + 发射头（`info_lt` 青点）。**`emp_tower.gd` 目前没有旋转代码**，需要转的话要补一行 |
| 激光塔 `tower_laser.png` | 128×128 | ✅ 朝首个目标 | **8 个发射头围一圈 + 中央核心**（`#FFD964` 亮黄 → 白）。各向同性，转不转都对 |
| 特斯拉 `tower_tesla.png` | 128×128 | ❌ 静态 | **两个并排线圈**：厚圆环电极（左上受光 / 右下压暗，读成甜甜圈）+ 中心电青球（`#6FD6EA`）。**不要画径向辐条 —— 会读成齿轮** |
| 无人机基地 `tower_drone.png` | 128×128 | ❌ 静态 | 深色停机坪 + **H 标志**（银 `#A8C0CE`）+ 虚线圆 + 四角四边 8 盏安全黄指示灯。本身就自带"底座"，不需要再叠 |

2×2 的三座塔（激光 / 特斯拉 / 无人机）需要底座时用 `base_large.png`（**104×104 < 128px 的塔位区**）。

> **拉手不能和炮管同向**：第一版把拉手画成横杆，和炮管同高同色，两者在视觉上连成
> 一根贯穿全塔的长条，舱盖完全读不出来。改成垂直于炮管之后才立住。
> 同理，任何"细长深色构件"都不要和炮管平行且等宽 —— 旋转时它们会到处粘连。

---

## 7. 字体与图标

**字体**：现在只有 `AlibabaPuHuiTi-3-85-Bold` 一个字重。
在中性灰面板上颜色已经分不出层级（语义色全在 3.0~4.0 区间），
**补一档 Regular 让层级靠字重承担**的优先级已经很高。

**图标**：现有图标风格混杂（线稿、实心、彩色混在一起）。统一为 2px 等宽描边的工业线稿，
颜色 `#DCEAF2`，激活态转 `#FFC61A`。

欢迎页四个按钮的图标色：

| 按钮 | 色 |
| --- | --- |
| 教程 | `#7FD0C6` 青（信息） |
| 开始游戏 | `#FFC61A` 安全黄（主操作） |
| 设置 / 关于 | `#DCEAF2` 银（次要） |

---

## 8. 落地顺序

1. ✅ **UI 主题迁移到深银金属**（v2.0）
2. ✅ **地板 / 背景 / 清屏色统一到 `#333C57`**（v3.2）
3. ⬜ **加一档正文字重**（优先级已提高）
4. ⬜ **统一图标**
5. ⬜ **世界美术实体化**：把占位色块换成真正的 TileSet（地砖、流水线、基座、出口）
   - ✅ 地砖：`sprite/tile/floor_*` 12 种
   - ✅ 塔位基座：`sprite/tile/floor_slot.png`
   - ✅ 流水线皮带：`sprite/tile/belt_*` 12 个方向
   - ⬜ 出口门 + 危险区、装饰层（管道 / 集装箱 / 隔断 / 警示牌）
   - ⬜ 把 `allowArea` 与路径接到瓦片上（关卡里现在还是旧的「绿草地 + 灰马路」tileset）
6. ⬜ 用新规范重做 `level_1` 作为样板关，再铺开其余关卡

---

## 9. 版本演进

| 版本 | 面板底 | 世界 | 问题 |
| --- | --- | --- | --- |
| v1.0 | `#F7F9FA`（0.94） | `#CFCBC4` 暖灰 | UI 过亮、飘，缺机械重量 |
| v2.0 | `#242A36`（0.023） | `#8C9195`（0.281） | 金属感对，但世界偏亮 |
| v3.2（当前） | **`#566C86`（0.144）** | **`#333C57`（0.046）** | — |

### v2.0 → v3.2 的关键变化

地板换成 Tech Dungeon tileset 最右区域的两色地砖后，地板明度从 0.281 掉到 0.046，
**原来的深面板 `#242A36`（0.023）和地图内面板 `#3A404F`（0.051）直接和地板撞在一起**
（差 0.023 / 0.005，几乎不可见）。

解决：把面板整体提到同色板的中灰 `#566C86`（差 0.098）并用 `#94B0C2` 2px 亮边框界定；
文字色随之整体提亮一档（次要文字从 `#A8AFA8` → `#DCEAF2`，
因为原来在中灰面板上只有 2.49 的对比度）。

---

## 10. 迁移验证（2026-09-13，真实运行）

| 检查项 | 方法 | 结果 |
| --- | --- | --- |
| 主题文字色 | 遍历 `theme.tres` 全部 18 个 `*/font_color` | 全部亮色；禁用类为压暗中间调 |
| StyleBox 面色 | 遍历 `theme/style/*.tres` | 20 个"面"全部落在深金属区间 |
| **调色板溯源** | 主题面色投影到 Sweetie 16 灰阶折线，算最大通道偏差 | 面板 0.000 / 按钮 0.010 / 悬停 0.012 / 按下 0.000 / 地图内面板 0.000 / 小标签 0.000 |
| 清屏色 | 读 `ProjectSettings` | `#333C57` = 车间地面色 |
| 背景场景 | 检查根节点 / 贴图 / 参数 | `TextureRect` + `background_tiled.png` + `speed=0.10` + `darkness=0.80`，滚动效果保留 |
| 背景色调 | 渲染实测 y=0.75 平均色 | `#313953` vs 目标 `#333C57`，偏差 **0.018** |
| 背景滚动 | 8 次不等间隔采样，与首帧比对 | 最大差异 **48960 个采样点**，确认在滚 |
| 三层明度 | 地板 / 面板 / 边框 | 0.046 / 0.144 / 0.412（差 0.098 / 0.268） |
| 文字对比度 | 8 组前景/背景算 WCAG 比值 | 主文字 5.20 ✅；语义色 3.0~3.95（大字级）；压暗 2.52（禁用态） |
| 面板实拍 | 渲染后取面板内像素**中位数** | 暂停 0.098 / 设置 0.141 / 结算 0.139 / 塔详情 0.139 / 敌人详情 0.139 |
| 文字可见 | 同一次采样取 **98 分位** | 0.78 ~ 0.96 |
| 转场 / 弹窗层级 | 重跑既有两套回归 | 45 + 26 项断言，**0 失败** |

**改动范围**：`theme/theme.tres`、`theme/style/` 全部 31 个 StyleBox、
15 个 `.tscn` 的硬编码文字色与内联 StyleBox、`achievement_panel.gd` 的颜色常量、
`shader/background.gdshader`（加 `tint`，滚动/平铺/渐变结构不变）、
`scene/bg.tscn`、`project.godot` 清屏色。
**没有任何 `.tscn` 的结构改动**。

---

## 11. 下一步待确认

- [ ] 面板 `#566C86` 的明度合适吗？偏亮就退回 `#4A5F78`，偏暗就往 `#6F8AA4` 走
- [ ] 语义色（黄/红/青/绿）在中灰面板上只能到"大字"级，是否接受？
     不接受的话需要给数值加暗底衬，或者补 Regular 字重
- [ ] 下一步优先做**真正的 TileSet**（把占位色块换成地砖素材）还是**字重 + 图标**？
- [x] `sprite/tile/` 的 11 种地砖：`floor_base` / `floor_vent` / `floor_drain`
      是手绘原稿（Aseprite 源文件留在 `E:\绘制的图片`，**不放进工程**），
      其余 8 种按同一色板补齐 —— 2026-09-13 确认**全部保留**。
- [x] `floor_vent` 的格栅节奏已规整为 6/6/6px + 上下各 14px（2026-09-13，只动 2 行像素），
      已写进 `tile_check` 回归断言。
- [ ] 接下来是继续补地砖（管道穿地 / 集装箱阴影 / 水渍），
      还是先把 `level_1` 的地面换成新瓦片做样板关、再铺开其余关卡？
