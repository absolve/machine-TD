# Machine-TD 项目现状与待开发功能分析

更新时间：2026-09-07

## 1. 项目概况

| 项目 | 当前情况 |
|---|---|
| 类型 | 2D 塔防游戏 |
| 引擎 | Godot 4.7，GL Compatibility |
| 主分辨率 | 1920×1080，窗口覆盖尺寸 960×540 |
| 语言 | GDScript |
| 主场景 | `scene/welcome.tscn` |
| 当前状态 | 核心塔防循环可运行，内容和系统仍在完善 |

项目采用 Godot Autoload 管理全局数据和跨场景流程，主要全局节点包括 `Game`、`StageData`、`UserData`、`TowerUpgradeManager`、`SceneTransition` 和 `AchievementManager`。

## 2. 当前结构

```text
machine-td/
├── autoload/       全局数据、存档、升级、场景切换、成就
├── scene/          游戏场景、塔、敌人、UI、关卡场景
├── script/         游戏逻辑脚本
├── sprite/         塔、敌人、UI 和地图资源
├── shader/         背景、塔升级等着色器
├── sound/          音效资源
├── theme/          全局主题和 StyleBox
└── lang/           中英文翻译资源
```

## 3. 已实现功能

### 3.1 核心流程

- 欢迎界面、关卡选择、地图战斗、结果弹窗、返回主菜单流程。
- 关卡数据集中配置于 [stageData.gd](machine-td/autoload/stageData.gd)。当前包含教程关和 1-15 关数据。
- 波次生成、敌人沿 `Path2D` 移动、敌人逃脱扣除基地生命、全部敌人消灭后通关。
- 胜利后记录评分、解锁下一关、发放首次通关宝石奖励。
- 场景切换使用全局 [sceneTransition.gd](machine-td/autoload/sceneTransition.gd)，当前为黑色淡入、等待异步加载、黑色淡出。

### 3.2 防御塔

当前塔类型：

| 塔 | 当前功能 | 状态 |
|---|---|---|
| 机枪塔 | 单目标高速直射 | 已实现 |
| 加农炮塔 | 单目标高伤直射 | 已实现 |
| 火箭塔 | 火箭弹和范围爆炸 | 已实现 |
| EMP 塔 | 雷达视觉和控制塔框架 | 部分实现，实际减速缺失 |
| 无人机基地 | 生成无人机并环绕攻击 | 已实现基础攻击流程 |
| 特斯拉线圈塔 | 链式闪电视觉 | 部分实现，伤害缺失 |
| 激光塔 | 最多 3 个目标，红色单线激光 | 已实现基础攻击和随机亮度视觉 |

已具备：

- 1×1 和 2×2 塔的网格占用。
- 放置阴影、合法区域检查、占用区域管理。
- 雷达检测目标、炮塔旋转、自动攻击。
- 选中塔显示攻击范围、塔下方血量/经验 UI、卖出按钮和出售金额。
- 每种塔从 `Game.towerInfo` 读取攻击、费用、冷却、范围、初始血量和最大血量。
- 塔等级、经验值和升级配置框架。
- 出售返还购买费用的一半。

### 3.3 敌人

已有敌人类型包括迷你坦克、中型坦克、重型坦克、装甲坦克、突击车、医疗车、自爆车、导弹车、侦察无人机和攻击直升机。

已具备：

- 基础血量、速度、奖励、护甲、飞行属性。
- 生命条、受伤、死亡和爆炸效果。
- 部分敌人攻击塔：中型坦克、导弹车、攻击直升机。
- 医疗车恢复附近敌人。
- 自爆车使用范围爆炸伤害。
- 敌人击杀奖励金币和经验。

### 3.4 UI 和玩家数据

- 欢迎页、设置页、关于页、暂停菜单、结果弹窗。
- 塔选择卡片、塔信息面板、扁平像素风图标展示攻击/费用/冷却/范围/血量。
- 关卡卡片和星级评分。
- HUD 基地生命、金币、波次、分数、开始/暂停、音效和音乐控制。
- Toast 提示、生命条、波次进度条。
- 中英文翻译 CSV 和 Godot Translation 资源。
- `UserData` 使用 `user://player_data.cfg` 保存：分数、宝石、关卡解锁、关卡评分、成就。
- 每个关卡有独立 `gemReward` 字段，首次通关时获得，重复通关不重复领取。

## 4. 当前明确缺失或未完成的功能

### P0：影响核心玩法

1. **EMP 塔没有实际减速**
   - 文件：[emp_tower.gd](machine-td/script/emp_tower.gd)
   - 当前主要是范围/扫描视觉，未对敌人的移动速度或攻击频率产生稳定影响。

2. **特斯拉线圈没有实际伤害**
   - 文件：[tesla_coil_tower.gd](machine-td/script/tesla_coil_tower.gd)
   - `fire_lightning()` 只完成目标和闪电视觉，链式伤害仍是 TODO。

3. **敌人攻击塔的实现不完整**
   - 只有部分敌人具备攻击逻辑，缺少统一的敌人攻击基类、目标选择、攻击冷却和攻击反馈。
   - 普通敌方子弹对塔的碰撞、失效目标清理和伤害类型需要实际运行验证。

4. **塔升级数据传递需要修复/验证**
   - [map.gd](machine-td/script/map.gd) 放置塔时应明确设置 `temp.type = type`。
   - 若未设置，塔实例可能保持 `tower.gd` 默认的机枪塔类型，导致其他塔升级时读取错误配置。

### P1：影响完整体验

1. **1X/2X 游戏速度未完成**
   - [map.gd](machine-td/script/map.gd) 的 `speedOn()` 和 `speedOff()` 为空。
   - 需要设置 `Engine.time_scale`，并同步按钮状态和暂停逻辑。

2. **成就没有接入实际事件**
   - `AchievementManager` 有配置、读取和展示逻辑，但击杀、通关、建塔、出售、升级等事件没有完整调用进度接口。

3. **统一音效系统未接入**
   - [sound_manage.gd](machine-td/script/sound_manage.gd) 的播放函数仍为空。
   - 建塔、射击、命中、爆炸、升级、按钮点击等音效尚未形成统一调用链。

4. **结果弹窗的重试回调为空**
   - [result_screen.gd](machine-td/script/result_screen.gd) 中 `_on_btn_restart_pressed()` 仍为占位函数。
   - 当前主要由 [map.gd](machine-td/script/map.gd) 外部连接按钮，需要统一职责。

5. **游戏统计不完整**
   - 分数主要在通关时按评分增加，击杀数、建塔数、塔损失、通关时间、用塔类型等统计尚未完整记录。

6. **关卡选择和下一关体验仍可加强**
   - “下一关”当前返回关卡选择界面，而不是直接加载下一关。
   - 最后一关之后应禁用或改变下一关按钮。
   - 可以在结果弹窗显示当前关卡、评分、宝石奖励、下一关解锁状态。

### P2：内容和长期维护

1. **11-15 关没有独立地图场景**
   - `stageData.gd` 中 11-15 关目前复用 `res://scene/level/level_1.tscn`。
   - 虽然波数和敌人配置不同，但路线、地图装饰和可放置区域没有变化。

2. **飞行单位体系没有完全统一**
   - [aircraft.gd](machine-td/script/aircraft.gd) 是基础框架，但部分空中敌人仍直接使用 `enemy.gd`。
   - 需要统一飞行移动、碰撞层、可攻击塔类型和空中路线。

3. **敌人类型设计和实际内容不一致**
   - 文档规划中的护盾坦克、炮兵、支援机、运输机、Boss、召唤和变身等类型尚未进入实际枚举、场景和生成流程。

4. **地图内容和关卡数据耦合较紧**
   - 关卡数据包含大量波次字典，场景路径和生成逻辑分散，后续增加 Boss、特殊事件和多路线时维护成本会增加。

## 5. 重要运行时风险

1. **动态敌人销毁使用 `owner.queue_free()` 的风险**
   - 多个敌人脚本依赖 `owner` 销毁节点。
   - 动态加入节点后 `owner` 不一定是预期对象，建议统一使用明确的敌人根节点引用或 `get_parent()` 约定。

2. **目标列表中的对象可能已经失效**
   - 部分塔在攻击前没有统一检查 `is_instance_valid()`。
   - 敌人死亡和退出雷达区域的时序可能留下无效引用。

3. **伤害类型没有完全贯通**
   - 激光塔调用 `enemy.hurt(atk, self)` 时没有传入 `"energy"`。
   - 如果护甲和能量伤害规则启用，激光应明确使用能量伤害类型。

4. **放置入口缺少最终合法性复核**
   - 预览时检查了网格，但 `map.placeTower()` 仍应再次确认占用区域没有越界、重叠或不在允许区域。

5. **通关计时和按钮状态需要防重复**
   - 通关回调、结果窗口按钮和场景切换应防止重复触发，避免重复奖励或重复切场景。

6. **存档失败需要实际运行验证**
   - 当前保存统一使用 `user://player_data.cfg`，代码有错误输出，但仍应在编辑器和导出版本分别验证创建、读取和升级存档。

## 6. 建议开发顺序

### 第一阶段：修复核心正确性

1. 设置 `temp.type = type` 并验证 7 种塔升级配置。
2. 修复敌人销毁引用，统一敌人死亡、逃脱和释放占用区域流程。
3. 实现 Tesla 实际链式伤害。
4. 实现 EMP 减速和对敌方攻击频率的控制。
5. 统一目标失效检查和伤害类型传递。
6. 复核存档、通关奖励和重复点击防护。

### 第二阶段：补齐核心体验

1. 实现 1X/2X 速度。
2. 接入攻击、爆炸、建塔、升级和按钮音效。
3. 接入成就事件。
4. 完善结果弹窗统计和下一关直达。
5. 增加塔被摧毁、敌人攻击基地等视觉和音效反馈。

### 第三阶段：扩充内容

1. 为 11-15 关制作独立地图和路线。
2. 完善空中单位基类和飞行路线。
3. 加入护盾、回血光环、远程炮击、召唤和 Boss 阶段。
4. 增加地图缩略图、关卡标签和难度提示。

## 7. 建议的最低测试清单

每次修改核心系统后至少验证：

- 七种塔都能正确放置、攻击、出售和读取自己的属性。
- 七种塔升级时读取各自的 `TowerUpgradeManager` 配置。
- 敌人死亡、逃脱、自爆和攻击塔后都能正确释放节点。
- EMP 能减速，Tesla 能造成链式伤害。
- 激光塔能对空，且使用能量伤害规则。
- 通关只发放一次宝石，重复通关不会重复奖励。
- 退出并重新启动后，关卡解锁、评分、宝石和设置仍然存在。
- 1X/2X、暂停、重试、下一关和返回主菜单不会重复触发场景切换。
- 大场景加载时保持黑屏，加载完成后再淡出。

## 8. 当前结论

项目已经具备可玩的塔防主循环、多个塔和敌人、关卡选择、存档、评分、宝石奖励以及基础 UI。当前最影响游戏完整度的不是界面数量，而是部分塔的核心效果尚未真正生效，以及敌我对象生命周期、伤害类型、速度控制和事件统计还没有统一。

下一步应优先完成 EMP、Tesla、塔类型传递和敌人生命周期修复，再进行音效、成就、Boss 和新地图内容扩展。